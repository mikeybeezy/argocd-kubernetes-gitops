apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grade-submission-test
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/mikeybeezy/argocd-kubernetes-gitops.git
    targetRevision: HEAD
    path: kustomize/overlays/test
  destination:
    server: https://kubernetes.default.svc
    namespace: grade-demo
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
